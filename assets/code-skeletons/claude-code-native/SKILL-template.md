---
name: {{skill-name}}
description: {{한국어로 100~200자. 트리거 키워드 명시 — 예: "X 분석", "Y 요약", "Z 만들어줘" 등 사용자가 입력했을 때 자동 트리거되어야 하는 상황 기술. 기본 동작 한 줄 명시}}
---

# {{Skill Title}}

> 이 스킬은 description의 트리거 키워드가 사용자 입력에서 발견되면 자동 실행됩니다.

---

## 핵심 행동 (한 문단)

{{1~3줄로 이 스킬이 무엇을 하는지. STEP 1 Use Case 답변에서 가져옴}}

## 성공 기준

{{STEP 1-C 답변에서 가져옴 — 정량/정성 모두}}

---

## 실행 절차

### Step 1 — {{첫 단계 이름}}
{{무엇을 하는지. 사용 도구 명시}}

### Step 2 — {{둘째 단계 이름}}
{{...}}

### Step 3 — {{셋째 단계 이름}}
{{...}}

각 단계 완료 즉시 다음으로 진행. 단계 사이에 분석가 승인이 필요한 지점은 명시 ("HITL — Plan Mode로 전환").

---

## 사용 도구

| 작업 | 도구 |
| :---- | :---- |
{{- STEP 10 답변에서 가져옴. 예시:}}
| 파일 읽기 | `Read` |
| 외부 시스템 조회 | `mcp__{{server}}__{{tool}}` |
| 분기 실행 | `Agent` (subagent_type=`{{subagent-name}}`) |

## 금지 행동 (감사 대상)

{{- STEP 7 블랙리스트 그대로}}
{{- 예: DB는 read-only — INSERT/UPDATE/DELETE 금지}}
{{- 예: 외부 발송은 명시 승인 후에만}}
{{- 예: PII 검출 시 자동 마스킹}}

## 산출물 저장 위치

- `{{output dir}}/` — {{결과물 종류 및 형식}}

## Evaluator (재검증)

{{STEP 2 #5·STEP 8 답변에서 가져옴 — 예: kpi-evaluator 서브에이전트가 PASS/FAIL 판정. FAIL 시 {{N}}회 재시도, 초과 시 escalation}}

## 워크플로우 한 줄

```
{{진입 → Step 1 → Step 2 (Evaluator) → ... → 최종 산출물 → Plan Mode 승인}}
```
