# {{프로젝트 이름}} — {{한 줄 설명}}

{{프로젝트 목적 1~2문장. STEP 1 Use Case + 성공 기준 요약}}

아키텍처 명세: `{{프로젝트명}}-architecture-spec.md`

---

## 도메인 컨텍스트

{{- 산업·도메인 특수 가정 — 예: 회계 기준(K-IFRS), 회계 연도(1~12월), 분기 표기(YYYYQN)}}
{{- 통화·언어·시간대 기본값}}
{{- 외부 참조 — 예: KPI 정의는 references/kpi-definitions.md}}

## 사용자

- **1차**: {{주 사용자}} — {{무엇을 위해. 자동화 목표 정량 명시}}
- **2차**: {{부차 사용자}} — {{무엇을 위해}}

## 성공 기준

{{- STEP 1-C 답변 그대로 — 예: 보고서 생성 시간 70% 감소, 분석 정확도 99% 이상}}

---

## 행동 규칙 (감사 대상)

1. **{{핵심 규칙 1}}** — {{예: DB는 항상 read-only. PreToolUse 훅이 차단함. LLM 의지로 우회 불가}}
2. **{{외부 발송은 명시 승인 후에만}}** — {{자동 발송 금지. 초안만 저장}}
3. **{{PII 검출 시 자동 마스킹}}** — {{주민번호·여권번호·전화번호 패턴은 [REDACTED] 처리 후 Evaluator 재검증}}
4. **{{모든 수치는 출처 명시 필수}}** — {{출처 없는 수치는 Evaluator가 FAIL 처리}}
5. **{{외부 API 호출 범위 제한}}** — {{permissions.deny로 차단}}

## 우선 도구·에이전트

| 작업 | 사용 |
| :---- | :---- |
| {{진입}} | `/{{command-name}}` |
| {{역할 1}} | `{{subagent-1}}` sub-agent |
| {{역할 2 (Critic)}} | `{{evaluator-subagent}}` sub-agent |
| {{역할 3}} | `{{subagent-3}}` sub-agent |
| {{외부 시스템 조회}} | `mcp__{{server}}__{{tool}}` |

## 산출물 저장 위치

- `{{input dir}}/` — {{원본 입력. read-only}}
- `{{output dir}}/` — {{생성 결과물. 분석가 검토 대기}}
- `.claude/logs/{{audit log}}` — {{모든 도구 호출 감사 로그. PostToolUse 훅이 자동 기록}}

## 워크플로우 한 줄

```
/{{command-name}} {{인자}}
  → 1) {{서브에이전트 1 위임}} → 2) {{Evaluator 검증}} → 3) {{보강}} → 4) {{최종 합성}} → 5) Plan Mode로 분석가 승인
```

Evaluator FAIL 시 {{N}}회까지 재시도. 초과 시 분석가에게 escalation.
